import { createStore } from 'vuex';

import navPanel from './modules/navPanel';

export default createStore({
  state: {
  },
  getters: {
  },
  mutations: {
  },
  actions: {
  },
  modules: {
    nav: navPanel
  }
});
